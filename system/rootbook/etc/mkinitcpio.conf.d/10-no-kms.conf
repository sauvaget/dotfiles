# Arch default hooks without "kms": it copies firmware for every Intel GPU generation
# (~11 MB) into the initramfs, which the 100 MB EFI partition shared with Windows can't spare.
# The i915 driver then loads from the root filesystem instead (brief resolution switch at boot).
HOOKS=(base systemd autodetect microcode modconf keyboard sd-vconsole block filesystems fsck)
