[terminal]
# The VT to run the greeter on. Can be "next", "current" or a number
# designating the VT.
vt = 1

[default_session]
# Add --vt 1 to force it onto the first virtual terminal
command = "tuigreet --time --remember --cmd 'zsh -l -c sway'"
user = "greeter"

