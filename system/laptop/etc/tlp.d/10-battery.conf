# Stop charging at 80% to reduce battery wear.
# ASUS only supports a stop threshold; the start value is ignored.
# Charge to 100% once (until next boot): sudo tlp fullcharge   (zsh alias: fullcharge)
STOP_CHARGE_THRESH_BAT0=80
